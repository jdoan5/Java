meta {
  name: POST Create Ticket
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/tickets
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Macbook Pro Issue",
    "description": "The screen is damaged and needs to be replaced.",
    "priority": "HIGH"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
