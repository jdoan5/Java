apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookmark-api.fullname" . }}
  labels:
    {{- include "bookmark-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookmark-api.selectorLabels" . | nindent 4 }}
